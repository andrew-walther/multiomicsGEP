.Rproj.user
.Rhistory
.Rapp.history
.RData
.Ruserdata

# macOS metadata
.DS_Store
**/.DS_Store

# Tool config directories
.claude/
.claire/
.obsidian/

# PDAC data — stored locally in OneDrive, not in git
# Local path: ~/Library/CloudStorage/OneDrive-UniversityofNorthCarolinaatChapelHill/UNC Dissertation (Liu)/PDAC_data/
# Longleaf path: set via PDAC_DATA_ROOT env var
**/PDAC_data/
**/data/original/

# LaTeX build artifacts from Quarto PDF rendering
# Note: .tex files are source documents and are NOT ignored
**/*.aux
**/*.log
**/*.toc
**/*.out

# Quarto HTML build artifacts (self-contained HTML embeds these; folder not needed)
**/*_files/

# Annotated copies (local working copies, not version-controlled)
**/annotated copies/
.positai
# Large binary model outputs
results/**/*.rds
